# coding: utf-8

"""
    GraphSense API

    GraphSense API provides programmatic access to various ledgers' addresses, entities, blocks, transactions and tags for automated and highly efficient forensics tasks.  # noqa: E501

    Contact: contact@ikna.io
    Generated by: https://openapi-generator.tech
"""


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from graphsense.models.tx_account import TxAccount
from graphsense.models.tx_utxo import TxUtxo
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

TX_ONE_OF_SCHEMAS = ["TxAccount", "TxUtxo"]

class Tx(BaseModel):
    """
    Tx
    """
    # data type: TxUtxo
    oneof_schema_1_validator: Optional[TxUtxo] = None
    # data type: TxAccount
    oneof_schema_2_validator: Optional[TxAccount] = None
    actual_instance: Optional[Union[TxAccount, TxUtxo]] = None
    one_of_schemas: Set[str] = { "TxAccount", "TxUtxo" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = Tx.model_construct()
        error_messages = []
        match = 0
        # validate data type: TxUtxo
        if not isinstance(v, TxUtxo):
            error_messages.append(f"Error! Input type `{type(v)}` is not `TxUtxo`")
        else:
            match += 1
        # validate data type: TxAccount
        if not isinstance(v, TxAccount):
            error_messages.append(f"Error! Input type `{type(v)}` is not `TxAccount`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in Tx with oneOf schemas: TxAccount, TxUtxo. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in Tx with oneOf schemas: TxAccount, TxUtxo. Details: " + ", ".join(error_messages))
        else:
            return v
    def __getattr__(self, name: str):
        """Delegate attribute access to actual_instance for backward compatibility.

        This allows code like `tx.height` instead of `tx.actual_instance.height`.
        """
        if name.startswith('_') or name in (
            'actual_instance', 'one_of_schemas', 'model_config',
            'discriminator_value_class_map', 'model_fields', 'model_computed_fields',
            'model_extra', 'model_fields_set', 'oneof_schema_1_validator',
            'oneof_schema_2_validator', 'oneof_schema_3_validator'
        ):
            raise AttributeError(f"'{type(self).__name__}' object has no attribute '{name}'")

        actual = object.__getattribute__(self, 'actual_instance')
        if actual is None:
            raise AttributeError(f"'{type(self).__name__}' object has no attribute '{name}'")
        return getattr(actual, name)

    def __setattr__(self, name: str, value):
        """Delegate attribute setting to actual_instance for backward compatibility."""
        if name.startswith('_') or name in (
            'actual_instance', 'one_of_schemas', 'model_config',
            'discriminator_value_class_map', 'model_fields', 'model_computed_fields',
            'model_extra', 'model_fields_set'
        ):
            super().__setattr__(name, value)
            return

        try:
            actual = object.__getattribute__(self, 'actual_instance')
            if actual is not None:
                setattr(actual, name, value)
                return
        except AttributeError:
            pass

        super().__setattr__(name, value)

    def __getitem__(self, key):
        """Delegate subscript access to actual_instance for backward compatibility.

        This allows code like `tx['height']` instead of `tx.actual_instance['height']`.
        """
        actual = object.__getattribute__(self, 'actual_instance')
        if actual is None:
            raise KeyError(key)
        # Try dict-style access first, then attribute access
        if hasattr(actual, '__getitem__'):
            return actual[key]
        return getattr(actual, key)



    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into TxUtxo
        try:
            instance.actual_instance = TxUtxo.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into TxAccount
        try:
            instance.actual_instance = TxAccount.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into Tx with oneOf schemas: TxAccount, TxUtxo. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into Tx with oneOf schemas: TxAccount, TxUtxo. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], TxAccount, TxUtxo]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


